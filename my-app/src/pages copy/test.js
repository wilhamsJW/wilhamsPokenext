export default function NavBar() {
    return (
        <h1>Teste para acesssar uma página, todos os arquivos criado dentro de pages, atoumaticamente serão uma rota, q para acessar devemos usar
            o nome deste arquivo como a rota, esse arqivo se chama test.js e para acessálo basta colocar isso: href={`/test`}
            {/**<Link href={`/test`}>Teste rota</Link> no seu JSX*/}
        </h1>
    )
}